Infrastructure
==============

Carpeta para implementaciones concretas (NHibernate/EF Core) de repositorios y UnitOfWork.

Por ahora contiene sólo un esqueleto. En iteraciones posteriores se añadirá:
- Implementaciones NHibernate en `Infrastructure/NHibernate` y mappings `.hbm.xml`.
- Helpers para carga de `NHibernate.cfg.xml` y `SchemaExport`.

Seguir la convención descrita en `solution.plan.md`.
